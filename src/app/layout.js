import "./globals.css";
import { Montserrat, Noto_Serif } from 'next/font/google';
import Rodape from "@/components/rodape.jsx";
import Navbar from "@/components/navbar";
import FaleConosco from "@/components/faleConosco";

const montserrat = Montserrat({
  subsets: ['latin'],
  variable: '--font-montserrat',
});

const notoSerif = Noto_Serif({
  subsets: ['latin'],
  variable: '--font-noto-serif',
});

export const metadata = {
  title: "Magic Eventos",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${montserrat.variable} ${notoSerif.variable} h-full antialiased overflow-x-hidden`}
    >
      <body className="font-sans">

        <div className="relative min-h-screen bg-[#320A27] overflow-x-hidden">

          <div className="relative z-10 flex flex-col min-h-screen">

            <Navbar />

            {children}
              <div className="w-[310px] md:max-w-[1200px] mx-auto md:w-full">
                <FaleConosco id="contato" />
                <div className="md:mb-[150px] mt-10">
                  <Rodape />
              </div>
            </div>
          </div>

        </div>

      </body>
    </html>
  );
}